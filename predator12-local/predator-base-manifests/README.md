# predator-base-manifests

Bootstrap template for Predator manifests used by `scripts/gitops_sync.sh` when the manifests repo is empty.

This minimal template contains a simple helm chart `predator-umbrella` with `values.yaml` and a tiny deployment template.
