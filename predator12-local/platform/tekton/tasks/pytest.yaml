apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: pytest
spec:
  steps:
    - name: run-pytests
      image: python:3.11-slim
      script: |
        #!/usr/bin/env bash
        set -e
        pip install -r backend/requirements.txt || true
        pytest -q || exit 1
