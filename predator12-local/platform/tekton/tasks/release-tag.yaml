apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: release-tag
spec:
  params:
    - name: commit
  steps:
    - name: tag
      image: alpine/git
      script: |
        #!/bin/sh
        set -e
        echo "Creating signed tag for commit $(params.commit) (placeholder)"
        # Example: git tag -s v1.2.3 $(params.commit)
        exit 0
