{
  "name": "voice-controls",
  "displayName": "Voice Controls (UA)",
  "description": "Simple VS Code extension: open a webview to dictate (STT) and speak (TTS) in Ukrainian and insert text into the active editor.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.60.0"
  },
  "activationEvents": [
    "onCommand:voiceControls.open"
  ],
  "main": "extension.js",
  "contributes": {
    "commands": [
      {
        "command": "voiceControls.open",
        "title": "Voice Controls: Open"
      }
    ]
  }
}
