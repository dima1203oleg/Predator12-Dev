# 🎯 Predator12 - AI Система Обробки Даних

## 📋 Огляд

Predator12 — це детермінована платформа обробки даних з інтерактивним AI інтерфейсом, що гарантує ідентичність результатів для людини та машини. Система включає 3D AI обличчя, чат-інтерфейс і візуалізацію мережі зв'язків.

## 🚀 Швідкий Старт

### Встановлення

1. **Клонування репозиторію:**

```bash
git clone <repository-url>
cd Predator12
```

2. **Запуск через VS Code:**

- Відкрийте папку в VS Code
- Використайте доступні таски для запуску

3. **Запуск через браузер:**

- Відкрийте `frontend/index.html` в браузері
- Або запустіть локальний сервер

### Структура Проекту

```
Predator12/
├── frontend/                 # Фронтенд інтерфейс
│   ├── index.html           # Головна сторінка
│   ├── css/                 # Стилі
│   │   ├── predator-ui.css  # Основні стилі
│   │   ├── ai-face.css      # Стилі 3D обличчя
│   │   ├── chat-interface.css # Стилі чату
│   │   └── network-graph.css  # Стилі мережі
│   └── js/                  # JavaScript модулі
│       ├── main.js          # Головний модуль
│       ├── ai-face.js       # 3D AI обличчя
│       ├── chat-interface.js # Чат інтерфейс
│       └── network-graph.js # Мережа зв'язків
└── README.md               # Документація
```

## 🎭 Компоненти Системи

### 1. AI Face (Інтерактивне 3D Обличчя)

**Функції:**

- 3D рендеринг через Three.js
- Система емоцій (нейтральний, радісний, думаючий, говорячий, здивований, сумний)
- Fallback до 2D при проблемах з WebGL
- Адаптивна анімація та реакції

**Використання:**

```javascript
const aiFace = new AIFace("ai-face-container");
await aiFace.init();
aiFace.setEmotion("happy");
```

### 2. Chat Interface (Чат-Інтерфейс)

**Функції:**

- Інтерактивний чат з AI
- Голосовий синтез відповідей
- Обробка повідомлень користувача
- Інтеграція з AI обличчям
- Швидкі відповіді

**Використання:**

```javascript
const chatInterface = new ChatInterface("chat-container", {
  aiFace: aiFaceInstance,
  apiUrl: "/api/chat",
});
await chatInterface.init();
```

### 3. Network Graph (Візуалізація Мережі)

**Функції:**

- Інтерактивна візуалізація мережі зв'язків
- D3.js для створення графіків
- Зумування і панорамування
- Деталі вузлів і зв'язків
- Пошук і фільтрація

**Використання:**

```javascript
const networkGraph = new NetworkGraph("network-container");
await networkGraph.init();
networkGraph.updateData(networkData);
```

## 🔧 Налаштування

### Конфігурація API

```javascript
const config = {
  api: {
    baseUrl: "/api/v1",
    endpoints: {
      analyze: "/analyze",
      chat: "/chat",
      network: "/network",
    },
  },
};
```

### Налаштування Теми

```css
:root {
  --primary-color: #007acc;
  --secondary-color: #00d4ff;
  --background-color: #1a1a2e;
  --text-color: #e0e0e0;
}
```

## 💬 Інтеграція Компонентів

Всі компоненти інтегровані через головний модуль `main.js`:

```javascript
// Ініціалізація системи
const app = new Predator12App();
await app.init();

// Доступ до компонентів
const aiFace = app.aiFace;
const chat = app.chatInterface;
const network = app.networkGraph;
```

## 🎨 Емоції AI

| Емоція      | Опис             | Використання           |
| ----------- | ---------------- | ---------------------- |
| `neutral`   | Нейтральний стан | За замовчуванням       |
| `happy`     | Радісний         | Успішні операції       |
| `thinking`  | Обдумування      | Обробка запитів        |
| `speaking`  | Говорить         | Відповідь користувачу  |
| `surprised` | Здивований       | Несподівані результати |
| `sad`       | Сумний           | Помилки                |

## 🌐 Події та Взаємодія

### Слухачі Подій

```javascript
// Чат подія
chatInterface.on("messageResponse", (response) => {
  if (response.networkData) {
    networkGraph.updateData(response.networkData);
  }
});

// Мережа подія
networkGraph.on("nodeClick", (nodeData) => {
  const message = `Розкажи про: ${nodeData.label}`;
  chatInterface.addMessage(message, "user");
});
```

## ⌨️ Клавіатурні Скорочення

| Комбінація   | Дія                            |
| ------------ | ------------------------------ |
| `Ctrl+Enter` | Відправити повідомлення в чаті |
| `Escape`     | Скинути емоцію AI              |
| `F1`         | Показати довідку               |

## 📱 Адаптивність

Система повністю адаптивна для:

- Десктоп (1200px+)
- Планшет (768px - 1199px)
- Мобільний (до 767px)

## 🔍 Налагодження

### Включення Debug Режиму

```javascript
window.DEBUG = true;
```

### Логування

```javascript
console.log("🎯 Predator12 Debug:", data);
```

## 🚦 Статуси Системи

| Статус            | Опис                    |
| ----------------- | ----------------------- |
| `🟢 Онлайн`       | Всі компоненти працюють |
| `🟡 Завантаження` | Ініціалізація системи   |
| `🔴 Помилка`      | Критична помилка        |

## 📊 Формати Даних

### Структура Мережі

```javascript
const networkData = {
  nodes: [
    {
      id: "node1",
      label: "Назва вузла",
      type: "input|process|output|data",
      x: 100,
      y: 100,
    },
  ],
  links: [
    {
      source: "node1",
      target: "node2",
      weight: 1,
      type: "data-flow",
    },
  ],
};
```

### Повідомлення Чату

```javascript
const message = {
    content: 'Текст повідомлення',
    type: 'user|assistant|system',
    timestamp: Date.now(),
    metadata: {
        emotion: 'happy',
        networkData: {...}
    }
};
```

## 🛠️ Розробка

### Додавання Нових Емоцій

1. Додайте стилі в `ai-face.css`
2. Оновіть метод `setEmotion()` в `ai-face.js`
3. Додайте анімації при потребі

### Розширення Мережі

1. Додайте нові типи вузлів в `network-graph.js`
2. Оновіть стилі в `network-graph.css`
3. Додайте обробники подій

## 🔧 Технічні Вимоги

### Браузери

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

### Залежності

- Three.js (3D рендеринг)
- D3.js (візуалізація даних)
- Web Speech API (голосовий синтез)

## 📋 Чеклист Встановлення

- [ ] Клоновано репозиторій
- [ ] Відкрито в VS Code
- [ ] Перевірено структуру файлів
- [ ] Запущено `index.html`
- [ ] Перевірено роботу всіх компонентів
- [ ] Налаштовано API endpoints (за потреби)

## 🆘 Часті Проблеми

### Не працює 3D обличчя

- Перевірте підтримку WebGL в браузері
- Система автоматично перейде на 2D fallback

### Помилки завантаження

- Перевірте шляхи до файлів
- Переконайтеся що всі залежності завантажені

### Проблеми з голосом

- Перевірте дозволи браузера для мікрофона
- Web Speech API не підтримується в усіх браузерах

## 📞 Підтримка

Для отримання допомоги:

1. Перевірте секцію "Часті Проблеми"
2. Включіть debug режим
3. Перегляньте консоль браузера
4. Опишіть проблему детально

## 🎉 Готово!

Predator12 готовий до використання! Система забезпечує:

✅ Інтерактивний AI інтерфейс  
✅ 3D обличчя з емоціями  
✅ Повноцінний чат  
✅ Візуалізація мережі зв'язків  
✅ Адаптивний дизайн  
✅ Доступність і зручність

Насолоджуйтесь роботою з найсучаснішою AI системою обробки даних! 🚀
